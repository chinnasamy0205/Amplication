/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { CategoryMgtService } from "../categoryMgt.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CategoryMgtCreateInput } from "./CategoryMgtCreateInput";
import { CategoryMgtWhereInput } from "./CategoryMgtWhereInput";
import { CategoryMgtWhereUniqueInput } from "./CategoryMgtWhereUniqueInput";
import { CategoryMgtFindManyArgs } from "./CategoryMgtFindManyArgs";
import { CategoryMgtUpdateInput } from "./CategoryMgtUpdateInput";
import { CategoryMgt } from "./CategoryMgt";
@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class CategoryMgtControllerBase {
  constructor(
    protected readonly service: CategoryMgtService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "CategoryMgt",
    action: "create",
    possession: "any",
  })
  @common.Post()
  @swagger.ApiCreatedResponse({ type: CategoryMgt })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: CategoryMgtCreateInput
  ): Promise<CategoryMgt> {
    return await this.service.create({
      data: data,
      select: {
        createdAt: true,
        dateRefT: true,
        dateRefT1: true,
        id: true,
        incremental: true,
        status: true,
        totalCount: true,
        updatedAt: true,
        url: true,
        websiteName: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "CategoryMgt",
    action: "read",
    possession: "any",
  })
  @common.Get()
  @swagger.ApiOkResponse({ type: [CategoryMgt] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(CategoryMgtFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<CategoryMgt[]> {
    const args = plainToClass(CategoryMgtFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        createdAt: true,
        dateRefT: true,
        dateRefT1: true,
        id: true,
        incremental: true,
        status: true,
        totalCount: true,
        updatedAt: true,
        url: true,
        websiteName: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "CategoryMgt",
    action: "read",
    possession: "own",
  })
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: CategoryMgt })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: CategoryMgtWhereUniqueInput
  ): Promise<CategoryMgt | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        createdAt: true,
        dateRefT: true,
        dateRefT1: true,
        id: true,
        incremental: true,
        status: true,
        totalCount: true,
        updatedAt: true,
        url: true,
        websiteName: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "CategoryMgt",
    action: "update",
    possession: "any",
  })
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: CategoryMgt })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: CategoryMgtWhereUniqueInput,
    @common.Body() data: CategoryMgtUpdateInput
  ): Promise<CategoryMgt | null> {
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          createdAt: true,
          dateRefT: true,
          dateRefT1: true,
          id: true,
          incremental: true,
          status: true,
          totalCount: true,
          updatedAt: true,
          url: true,
          websiteName: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @nestAccessControl.UseRoles({
    resource: "CategoryMgt",
    action: "delete",
    possession: "any",
  })
  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: CategoryMgt })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: CategoryMgtWhereUniqueInput
  ): Promise<CategoryMgt | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          createdAt: true,
          dateRefT: true,
          dateRefT1: true,
          id: true,
          incremental: true,
          status: true,
          totalCount: true,
          updatedAt: true,
          url: true,
          websiteName: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
